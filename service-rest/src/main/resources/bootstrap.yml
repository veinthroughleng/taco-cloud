spring:
  profiles:
    active: dev, eureka, admin-eureka, property, jpa-rest, rest, security
#      active: dev, eureka, admin-eureka, property, message, rabbit, rabbit-sender, jpa-rest, rest, security
  #dev,prod,eureka,admin-eureka,property,jpa-rest,rest,message,rabbit,rabbit-sender,rabbit-receiver,rabbit-listener,mpa,security,feign-hystrix
  application:
    name: service-rest
  cloud:
    config:
      uri: http://localhost:8083