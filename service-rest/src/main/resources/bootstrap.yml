spring:
  profiles:
#    active: dev, eureka, admin, admin-eureka, property, message, rabbit, rabbit-sender, jpa-rest, rest, security
    # Non-messaging active profiles, for DEBUG purpose/ rabbit(cloudamqp.rabbit) service in Cloud Foundry is paid(not free)
    active: dev, eureka, admin, admin-eureka, property, jpa-rest, rest, security
  #dev,prod,eureka,admin,admin-eureka,property,jpa-rest,rest,message,rabbit,rabbit-sender,rabbit-receiver,rabbit-listener,mpa,security,feign-hystrix,jwt
  application:
    name: service-rest
  cloud:
    config:
      uri: https://veinthrough-service-config.cfapps.io